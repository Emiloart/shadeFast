# Supabase Workspace

- `migrations/`: SQL migrations for schema, indexes, and RLS policies.
- `functions/`: Edge Functions for protected write operations and moderation workflows.

Apply migrations via Supabase CLI once linked to your project.
